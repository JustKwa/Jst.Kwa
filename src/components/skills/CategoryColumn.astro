---
const { title, items = [] } = Astro.props;
---
<div class="flex flex-col">
  <h3 class="text-lg font-semibold mb-2">{title}</h3>
  <ul class="space-y-1">
    {items.map((item: string[]) => (
      <li class="text-gray-700 text-left">{item}</li>
    ))}
  </ul>
</div>
